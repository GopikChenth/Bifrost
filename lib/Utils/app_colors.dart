import 'package:flutter/material.dart';

abstract class AppColors {
  static const Color primary = Color(0xFF52A435);          // main actions, buttons
  static const Color primaryDark = Color(0xFF3F7F29);      // pressed buttons, hover states
  static const Color primaryLight = Color(0xFF8CCF73);     // highlights, chips, selected items
  static const Color backgroundDark = Color(0xFF171615);   // background dark
  static const Color surface = Color(0xFF232120);          // cards, containers
  static const Color border = Color(0xFF353332);           // outline / border / divider
  static const Color textPrimary = Color(0xFFFFFFFF);      // primary text
  static const Color textSecondary = Color(0xFFB7B7B7);    // secondary text
  static const Color accent = Color(0xFFE6B84A);           // gold/yellow for notifications, important elements
}
